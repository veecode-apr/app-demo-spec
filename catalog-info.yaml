apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: app-demo-spec-location
  description: Location to import all app-demo-spec catalog components
spec:
  targets:
    - ./.platform/*.yaml
